# 审查修复续接记录

根据导出的会话恢复“开始修复”任务，保留并完成中断前的工作区改动。

## 已修复

- 回复准入使用候选收件人阈值，避免基线与自身比较。
- 缺失分数或未知前序 Bot 上下文排除出可回放指标、Bootstrap、分组诊断及样本门槛；不可用数量单列。
- 缺失准入判定不生成虚假的 silent 样本；来源未声明不计为纯人工。
- 去重特征名；旧模型重复权重在对齐时相加，保持预测分数。
- 调参置信度依据有效留出样本数；状态历史保留 proposed 起点。
- 离线 forward 结果不冒充在线影子观察；shadow_entered 仅表示经历过状态阶段。
- 分析移到工作线程；清空数据同时清除契约快照和复盘缓存。
- 保留宿主明确的 outcome 阶段和 trace/record 来源。
- 导出完整配置；未知运行快照版本按结构读取并报告版本差异。
- 缺失宿主窗口限额和时钟时保留未知，不推算虚假过期时间。
- 前端修复合法策略操作、分页、超时、部分请求失败、重复刷新、过期响应、复盘缓存、失败提示及标签页切换。
- 本工作区的 ChatDynamics 副本保留 shadow.recorded_at，配有跨仓库回归。
- 更新契约中 should_reply 和候选 evidence 的说明。

## 审查结论的边界

- 当前宿主运行快照版本仍是 1；版本升级断流属于兼容风险，不是已发生的故障。
- 无任何标注时间戳时仍阻止策略通过数据门槛：无法验证数据新鲜度，不能直接放行。门槛会明确说明原因。
- 未清除仅被列为未使用的辅助函数；未使用本身不证明存在功能错误。
- 未引入跨运行缓存；工作线程解决同步阻塞，未声称消除大数据集的计算成本。
- shadow_observed 当前保守为 false，因为策略记录尚未持久化匹配的在线观测证据。真实统计从 /shadow 查看。

## 验证范围

Python 回归覆盖学习层及宿主 trace 写出契约；Node 回归和真实 Chromium 的本地 bridge 模拟覆盖页面交互。
真实 AstrBot SDK 已验证插件导入；未连接运行中的 AstrBot 验证完整宿主页面流程。
修改过的核心模块定向 mypy 通过；全项目 mypy 仍有既有 Web API 注解和当前系统 Python 缺失 SDK 依赖等问题。

修复版为 v1.3.2；宿主补丁随发布包附带，学习层不会自动修改宿主。
